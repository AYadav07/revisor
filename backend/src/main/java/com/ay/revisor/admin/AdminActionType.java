package com.ay.revisor.admin;

/** Stored by name in {@code admin_action.action}. */
public enum AdminActionType {
    LIST_USERS,
    ENABLE_USER,
    DISABLE_USER,
    DELETE_USER,
    VIEW_USER_COURSES
}
